<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">

	<select id="userList" resultType="UserVo">
	select * from users order by userid
	</select>



	<select id="userInfo" parameterType="String" resultType="UserVo">

select * from users where userid = #{userId}


	</select>


	<!-- pageVo int page int pageSize -->
	<select id="userPagingList" parameterType="pageVo" resultType="UserVo">
<![CDATA[

		select *
		from
		(select a.*, rownum rn
		from
		(select a.*
		from users a
		order by
		 userid) a)
		where 	rn >= (#{page}-1)*#{pageSize}+1 and rn<= #{page}*#{pageSize}
	
]]>

	</select>
	
	<!-- 사용자 전체 수 조회  -->

	<select id="usersCnt" resultType="int">
	 select count(*) from users
	</select>




	 <insert id="insertUser" parameterType="UserVo" >
	 	insert into users
		VALUES (#{userId}, #{name}, #{alias}, #{pass}, #{addr1}, #{addr2},#{zipcd},#{birth},'','' )
	 </insert>


<update id="updateUser" parameterType="UserVo">
update users set alias = #{alias}, pass = #{pass}, addr1 = #{addr1}, addr2 = #{addr2}, zipcd = #{zipcd} where userid = #{userId}
</update>





<delete id="deleteUser"  parameterType="String">

delete from users
 where userid = #{userId}
 
</delete>













	<select id="lprodList" resultType="lprodVo">


		select * from lprod 
	</select>




	<!-- pageVo int page int pageSize -->
	<select id="lprodPagingList" parameterType="pageVo" resultType="lprodVo">
<![CDATA[

		select *
		from
		(select a.*, rownum rn
		from
		(select a.*
		from lprod a
		order by
		 lprod_id) a)
		where 	rn >= (#{page}-1)*#{pageSize}+1 and rn<= #{page}*#{pageSize}
	
]]>

	</select>
	
	<!-- 사용자 전체 수 조회  -->

	<select id="lprodCnt" resultType="int">
	 select count(*) from lprod
	</select>




</mapper>